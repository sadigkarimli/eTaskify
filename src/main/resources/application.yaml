server:
  port: 8081
  error:
    whitelabel:
      enabled: false
    include-stacktrace: never

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: true
    generate-ddl: true

  datasource:
    url: jdbc:postgresql://localhost:5433/etaskify
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin

  mail:
    host: smtp.gmail.com
    port: 587
    username: noreply.etaskify@gmail.com
    password: DXF4VISS
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  liquibase:
    default-schema: etaskify
    enabled: false

springdoc:
  swagger-ui:
    operations-sorter: method
